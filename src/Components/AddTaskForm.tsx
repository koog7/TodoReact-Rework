
const AddTaskForm = () => {
    return (
        <div>
            <form>
                <input type={"text"} placeholder={'Write a task'}/>
                <button type={"submit"}> Send</button>
            </form>
        </div>
    );
};

export default AddTaskForm;